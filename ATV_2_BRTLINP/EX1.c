#include <stdio.h>

int main(void) {
  int x = 2; 
  int y = 3;
  int sub = x - y;
  printf("Os numeros escolhidos foram %d e %d \n", x, y);
  printf("Resultado da operação de %d - %d é igual a %d", x, y, sub);
  return 0;
}